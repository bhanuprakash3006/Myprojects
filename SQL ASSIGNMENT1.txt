1Q.
 SELECT SUBSTRING(EMAIL,1,5) FROM employees; or SELECT LEFT(EMAIL,5) FROM employees; or SELECT EMAIL FROM employees WHERE EMAIL LIKE '_____';
2Q.
SELECT COUNTRY_ID, COUNT(CITY) FROM locations GROUP BY COUNTRY_ID;
3Q.
SELECT REGION_ID, COUNT(REGION_ID),REGION_NAME FROM countries NATURAL JOIN regions GROUP BY REGION_ID order by COUNT(REGION_ID) DESC LIMIT 1;
4Q.
SELECT EMPLOYEE_ID FROM employees WHERE FIRST_NAME LIKE 'J%' AND SALARY>10000;
5Q.
SELECT CITY FROM locations WHERE CITY LIKE '%South%';
6Q.
SELECT DEPARTMENT_ID,DEPARTMENT_NAME,SUM(SALARY) FROM employees JOIN departments USING(DEPARTMENT_ID) GROUP BY DEPARTMENT_NAME;
7Q.
SELECT DEPARTMENT_ID,DEPARTMENT_NAME,COUNT(EMPLOYEE_ID) FROM employees JOIN departments USING(DEPARTMENT_ID) GROUP BY DEPARTMENT_NAME;
8Q.
SELECT FIRST_NAME,LAST_NAME,DEPARTMENT_NAME FROM employees JOIN departments USING(DEPARTMENT_ID) JOIN job_history USING(DEPARTMENT_ID) GROUP BY DEPARTMENT_NAME ORDER BY DATEDIFF(END_DATE,START_DATE) DESC;
9Q.
SELECT FIRST_NAME,LAST_NAME FROM employees WHERE LENGTH(LAST_NAME) = 5;
10Q.
SELECT MANAGER_ID,COUNT(EMPLOYEE_ID) FROM employees GROUP BY MANAGER_ID;